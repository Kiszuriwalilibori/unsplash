import { styled } from "@mui/material/styles";

export const HintsContainer = styled("section")(({ theme }) => ({
    paddingRight: theme.spacing(1.5),
    maxWidth: "1320px",
    marginLeft: "auto",
    marginRight: "auto",
    marginTop: "12px",
}));
